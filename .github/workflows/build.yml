name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-22.04, windows-latest]

    runs-on: ${{ matrix.platform }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Rust (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        run: npm install

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Kiro Account Manager ${{ github.ref_name }}'
          releaseBody: |
            ## 🎉 Kiro Account Manager ${{ github.ref_name }}
            
            ### 📦 下载说明
            - **Windows**: 下载 `.msi` 或 `.exe` 文件
            - **macOS (Intel)**: 下载 `x64.dmg`
            - **macOS (Apple Silicon)**: 下载 `aarch64.dmg`
            - **Linux**: 下载 `.AppImage` 或 `.deb`
            
            ### ✨ 主要功能
            - 🔐 多账号管理
            - 👥 用户注册/登录系统
            - 🛡️ 管理员后台面板
            - 📊 账号分配管理
            - 🎨 多主题支持
            - 🌍 多语言支持
            
            ### 📝 更新日志
            查看完整更新日志请访问 [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md)
          releaseDraft: false
          prerelease: false
